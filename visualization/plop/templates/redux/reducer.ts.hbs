import { {{properCase name}}Action, {{properCase name}}Actions, set{{properCase name}} } from "./{{camelCase name}}.actions"
import { clone } from "../../../util/clone"

export function {{camelCase name}}(state = set{{properCase name}}().payload, action: {{properCase name}}Action) {
	switch (action.type) {
		case {{properCase name}}Actions.SET_{{constantCase name}}:
			return action.payload
		default:
			return state
	}
}
