import { UpdateVisibleTopLabelsEffect } from "./updateVisibleTopLabels/updateVisibleTopLabels.effect"

/** The labelSettings feature's ngrx effects, registered by the app composition root (Slice 15c). */
export const labelSettingsEffects = [UpdateVisibleTopLabelsEffect]
