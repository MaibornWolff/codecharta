import { {{properCase name}}Action, {{properCase name}}Actions } from "./{{camelCase name}}.actions"
import _ from "lodash"

export function {{camelCase name}}(state: {{type}} = {{default}}, action: {{properCase name}}Action): {{type}} {
	switch (action.type) {
		case {{properCase name}}Actions.SET_{{constantCase name}}:
			return _.cloneDeep(action.payload) //TODO: _.cloneDeep not required for primitives
		default:
			return state
	}
}
