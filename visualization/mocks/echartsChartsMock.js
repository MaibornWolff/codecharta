// Stub for `echarts/charts`. Kept apart from echartsMock.js: one stub file per entry point keeps each its own
// module identity, so a spec that jest.mocks one entry point does not silently replace the others.
module.exports = { SunburstChart: {}, CustomChart: {} }
